import { Link } from "react-router-dom";
import { Post } from "@/data/blog";
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar";
import { Badge } from "@/components/ui/badge";

interface FeaturedBlogPostProps {
  post: Post;
}

const FeaturedBlogPost = ({ post }: FeaturedBlogPostProps) => {
  return (
    <Link to={`/blog/${post.slug}`} className="block group">
      <div className="grid lg:grid-cols-2 gap-8 lg:gap-12 items-center bg-secondary/30 rounded-2xl p-4 lg:p-8 border">
        <div className="overflow-hidden rounded-xl">
          <img 
            src={post.imageUrl} 
            alt={post.title}
            className="w-full h-full object-cover aspect-video group-hover:scale-105 transition-transform duration-300"
          />
        </div>
        <div className="space-y-4">
          <Badge variant="outline">{post.category}</Badge>
          <h2 className="text-3xl lg:text-4xl font-bold text-foreground group-hover:text-primary transition-colors">
            {post.title}
          </h2>
          <p className="text-muted-foreground text-lg">
            {post.excerpt}
          </p>
          <div className="flex items-center gap-4 pt-2">
            <Avatar>
              <AvatarImage src={post.author.avatarUrl} alt={post.author.name} />
              <AvatarFallback>{post.author.name.charAt(0)}</AvatarFallback>
            </Avatar>
            <div>
              <p className="font-semibold text-foreground">{post.author.name}</p>
              <p className="text-sm text-muted-foreground">
                {post.publishedAt} · {post.readingTime} min read
              </p>
            </div>
          </div>
        </div>
      </div>
    </Link>
  );
};

export default FeaturedBlogPost;
