import { Plus, Upload, MoreVertical, File, Book, Video, Image as ImageIcon } from "lucide-react";
import { Button } from "@/components/ui/button";
import { materials, Material } from "@/data/workspace";
import { ScrollArea } from "@/components/ui/scroll-area";
import { toast } from "../ui/sonner";

const Sidebar = () => {

  const handleUpload = () => {
    toast.info("File Upload", {
      description: "This is where a file selection dialog would open.",
    });
  };

  return (
    <div className="w-72 bg-secondary/30 border-r flex flex-col h-full">
      <div className="p-4 border-b">
        <h2 className="text-lg font-semibold">Materials</h2>
      </div>
      <div className="p-4">
        <Button className="w-full" onClick={handleUpload}>
          <Upload className="w-4 h-4 mr-2" />
          Upload Files
        </Button>
      </div>
      <ScrollArea className="flex-1">
        <div className="p-4 space-y-2">
          {materials.map((item: Material) => (
            <div key={item.id} className="flex items-center p-2 rounded-lg hover:bg-secondary cursor-pointer group">
              <item.icon className="w-5 h-5 mr-3 text-muted-foreground" />
              <div className="flex-1">
                <p className="text-sm font-medium truncate">{item.name}</p>
                <p className="text-xs text-muted-foreground">{item.size} - {item.createdAt}</p>
              </div>
              <Button variant="ghost" size="icon" className="w-8 h-8 opacity-0 group-hover:opacity-100">
                <MoreVertical className="w-4 h-4" />
              </Button>
            </div>
          ))}
        </div>
      </ScrollArea>
      <div className="p-4 border-t mt-auto space-y-2">
         <Button variant="ghost" className="w-full justify-start">
            <Book className="w-4 h-4 mr-2"/> Teacher Tools
        </Button>
         <Button variant="ghost" className="w-full justify-start">
            <Plus className="w-4 h-4 mr-2"/> New Workspace
        </Button>
      </div>
    </div>
  );
};

export default Sidebar;
